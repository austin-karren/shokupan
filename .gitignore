# Omarchy writes these when it migrates a config you've edited
*.bak.*

# Personal identity — see README. Never track these.
.gitconfig.local
.bashrc.local
.XCompose.local

# Secrets / local env
.env
